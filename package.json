{
  "name": "retailer-system",
  "version": "1.0.0",
  "description": "Full retail management system with professional UI",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && python -m http.server 3000\"",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && python -m http.server 3000\"",
    "install-all": "npm install && cd backend && npm install",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && python -m http.server 3000"
  },
  "keywords": [
    "retail",
    "management",
    "system",
    "dashboard",
    "authentication"
  ],
  "author": "RetailPro Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "mongoose": "^9.0.2"
  }
}
